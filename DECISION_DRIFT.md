# 设计决策摇摆记录 (DECISION_DRIFT.md)

> **用途**: 记录历史上反复变更、撤回、合并、拆分或默认值来回调整的设计决策。
> 这类记录不是普通 bug 复盘，而是提醒后续 agent：这些问题的团队立场尚未完全稳定，改动前必须先确认当前取向。
>
> **维护规则**:
> 1. 每条记录必须包含：决策点、摇摆历史、当前状态、改动前问。
> 2. 发现同一设计点在一次或多次任务中被反复推翻、重做、拆分又合并、默认值来回切换时，必须追加记录。
> 3. 如果当前状态已经收敛为架构、产品或协作硬约束，必须同步更新 `ARCHITECTURE.md`、`PRODUCT.md` 或 `AGENTS.md`，并在本文件标注已收敛位置。
> 4. 记录的是"为什么这里容易摇摆"，不要只写最后一次实现结果。

---

## 当前摇摆点

| 决策点 | 摇摆历史 | 当前状态 | 改动前问 |
| --- | --- | --- | --- |
| **业务AI vs 审核AI 是否分离** | 最初统一 -> 拆分独立配置 -> 增加开关 -> 默认关闭 | 可选独立审核AI，默认关闭 | "审核AI走独立模型还是复用业务模型？" |
| **思考模式(reasoning)开关** | 默认开 -> 发现响应太慢 -> 默认关 | 默认关闭，用户可手动开 | "当前场景是否需要思考模式？" |
| **中西医数据模型** | 最初只有西医 -> 加中医诊断 -> ID冲突修复 -> 中药不匹配药品库 | 中西医各自独立数据源和模板 | "改动涉及中医还是西医？数据源是否隔离？" |
| **事实核查的植入方式** | 独立组件 -> 嵌入问诊页 -> 嵌入语音结果 -> 嵌入聊天 | 多处嵌入，缺少统一组合机制 | "事实核查是页面级还是全局级？" |
| **症状选择入口** | 单一列表 -> +部位选择 -> +系统分类 -> +搜索 -> +自定义录入 | 三种模式并行 + 自定义兜底 | "新的选择方式是替代还是新增？" |
| **复诊配药候选、确认与等待承载** | 初版要求近一年最多 5 次中诊断与药品都重复至少 2 次 -> 最近 3 次慢病就诊 -> 近 90 天时间窗 -> 多慢病增加独立确认页并串行生成确认计划、病历 -> 医保周期核查拆出完整同药流水 -> 为降低等待取消独立确认生成，改为病历与核查项一次返回、结果页确认 -> 单次生成仍在胶囊阻断 10 秒左右，改为先进入结果页显示真实阶段 -> 实测内容仍需等完整 JSON 一次性出现，最终改为确定性预填 + 单次 SSE 分段更新，并把复诊核查移到主诊断锚定浮层 -> 细进度线与条外阶段文字感知仍弱，改为条内显示当前任务和真实阶段数的高状态条 -> 实际走查发现第 4/4 步填充仍约 60% 且条内文案与激活标签不一致，进一步统一阶段语义与视觉锚点 -> 实际使用发现强制核查与大卡片浮层增加回写阻力，改为紧凑非阻断参考 -> 进度收起后通用病历补充仍自动改写正文，改为 finalizing 覆盖界面落地且慢病禁用完成后二次补充 -> 混合慢病就诊的处方被整体过滤，改为后台 AI 辅助归类、医生逐药勾选后才纳入 -> 实际体验认为诊断范围已确认后再逐药勾选过于复杂，改为 high / medium 归类自动纳入并在结果页统一核对 -> 后台识别状态仍占用胶囊空间，进一步改为完全静默，不再展示 AI、匹配数或失败提示 -> 独立快速归类仍可能阻塞胶囊提交，最终合并为同一次慢病生成流的 `medication_scope` 阶段 | 已收敛为同一近 90 天查询上的双派生、单次流式模型生成与渐进结果页：自动沿用和病历生成受医生确认病种范围约束，药品周期核查独立读取全部同药处方；单一慢病直接生成，多慢病只在胶囊确认范围；同次多慢病且 HIS 无处方诊断关联时，由同一次慢病生成流只在历史诊断候选内归类，医生的诊断勾选即范围确认，high / medium 且落入已选诊断的历史药品自动进入 scoped candidate，low / 未归类 / 失败继续安全降级；胶囊不再逐药确认，不展示也不等待归类过程，结果页仍允许取消或调整并完成统一药品定稿。确定性历史事实先展示，病历/药品范围/核查/药品逐段到达，迟到响应经会话 token 拦截；顶部状态条覆盖服务端流与本地最终应用，激活标签同时决定条内文案和阶段数，最后一步约 88%，病历映射及药品信息稳定后才到 100% 并收起；慢病完成后不再启动通用病历补充。复诊核查替代慢病场景普通主诊核查，以紧凑浮层提供可选参考，未处理不阻断整份病历回写，只有医生显式选择的内容才增量写入病历，见 `PRODUCT.md` 3B 与 `ARCHITECTURE.md` 患者上下文基线 | "诊断勾选是否已作为唯一范围确认？部分选择时是否只自动纳入白名单内 high / medium 归类药品并保持归类过程静默，在结果页保留取消与定稿？是否又恢复了胶囊逐药确认、独立归类请求、状态展示、整单继承或把 AI 归类直接当成最终回写？" |
| **报告解读承载形态** | 初版只允许 HIS / SDK 单份报告进入独立窗口，以避免打断问诊；新增接诊后近期报告入口时，若继续为每份报告开独立窗口会割裂历史浏览与报告回诊 | 已收敛为“双入口共用能力”：外部单报告仍用独立窗口，风险胶囊进入主窗口报告工作台；工作台内完成历史浏览和单份解读，并可从与本次回诊关联的报告升级到诊疗方案，见 `PRODUCT.md` 3/3A 与 `ARCHITECTURE.md` 14、11.1/11.2 | "当前改动是在维护外部单报告窗口，还是在扩展应用内报告工作台？是否仍复用同一解读内核和正文组件？" |
| **报告工作台 AI 触发时机** | 初版选择报告即自动生成 AI 解读，原始报告只在生成等待期短暂展示；医生实际更需要先核对原始结果，再决定是否消耗 AI 解读 | 已收敛为“原始报告默认 + 医生手动触发”：选中报告只加载并展示结构化原始结果，点击“开始 AI 解读”后才调用模型；结果按报告做会话缓存，并可在原始报告 / AI 解读间切换，见 `PRODUCT.md` 3 与 `ARCHITECTURE.md` 1.3 | "选择报告是否会偷偷触发 AI？解读后是否仍能返回同一份原始报告？" |
| **药品一次剂量是临床剂量还是制剂数** | 提示词曾要求模型返回 `targetDose`，前端再换算为“几片几粒”；PHIS 实际回写字段却是 `doseOnce + unitDose` 临床剂量，旧 hydrate 曾把制剂比值 `1` 与质量单位 `g/mg` 拼接，报告回诊因而出现 `1g/1mg`；后续又发现模型总量、hydrate 后总量和库存校验时总量可能来自不同阶段 | 已收敛为：`targetDose` 先换算到 PHIS `unitDose`；片/粒/袋数只由程序参与包装总量计算，药品详情加载后必须重新归一并用最终总量校验库存，模型包装总量不进入处方事实，见 `PRODUCT.md` 药品推荐定稿安全约束与 `ARCHITECTURE.md` 第 10 条 | "本次药品是否完整经过库存对齐、详情 hydrate、剂量换算、标准频次/用法、程序总量和最终库存校验？" |
| **复诊配药现病史由规则模板还是动态确认生成** | 初版直接由模型/兜底模板生成“病情控制、依从性待核实”式现病史；医生案例更偏自然临床叙事，但自动补“规律服药、病情平稳、无不适”又会制造未确认事实；后续引入动态确认后，文字/语音补充曾被设计为再次生成确认项，实测增加等待且改变了医生已浏览的选项 | 已收敛为：病历草稿和最少核查项由模型一次生成，默认推荐不等于确认；医生在结果页点击选项后，客户端只增量写入该选项的 `recordText`，不再调用第二次病历模型，也不覆盖医生手工文本，见 `PRODUCT.md` 3B、`ARCHITECTURE.md` 患者上下文基线 | "本次改动会不会让推荐值未确认即进入病历，或再次引入第二次生成等待？" |
| **客户端保留本地/区域双模式还是固定服务端托管** | 开发初期同时维护客户端直连 LLM/语音/PMPHAI、本地 Tauri 代理与区域服务代理；随后区域模式成为默认，但开关、模型设置页、客户端密钥、Rust 代理和本地统计仍长期并存，形成两套出口与契约漂移风险 | 已收敛为服务端托管单一模式：AI、语音、知识库、Prompt/模板、反馈和审计只走签名 `/v1/*`；客户端不保存第三方凭据，上游地址也不再通过 bootstrap 下发。HIS Bridge、HIS Adapter、目录缓存和桌面原生能力继续保留，见 `PRODUCT.md`、`ARCHITECTURE.md`、`AGENTS.md` 当前架构基线 | "这是服务端托管业务能力，还是必要的本机 HIS/桌面基础设施？是否会重新引入客户端第三方出口？" |
| **Windows 7 是正式兼容线还是隔离验证 flavor** | 当前正式 MSI 由普通 Rust Windows target 与 Evergreen WebView2 构建，实际最低系统已是 Windows 10；院内仍可能存在 Win7 SP1 终端，但直接修改正式安装器会污染 UpgradeCode、更新通道和安全基线，长期业务分支又容易漏同步临床与安全修复。首轮先收敛为 Artifact-only 验证 flavor；实机验证后，业务决定不分源码分支、改用独立发布通道承接有限期更新；独立发布 CI 初版又在构建前探测内网 `latest.json`，发现 GitHub 托管 Runner 无法访问院内服务后改为离线前置版本输入与服务端最终门禁 | 已收敛为同一 `main` 业务代码、独立 `tauri.win7.conf.json` flavor 和两级流程：`Win7 Legacy Test Build` 保持 updater 关闭；`Win7 Legacy Release Build` 只用独立签名密钥生成供后台上传的签名包，发布到 `win7-testing / win7-production`。GitHub CI 不连接院内服务，`previous_version` 只做离线预检，内网 PCIE Server 按通道历史最高版本做最终单调递增校验，降级只走回滚。Win7 固定 Tier 3 target、外置 WebView2 109、独立 identifier / UpgradeCode / latest / policy / rollback，不共享普通 Windows `production / testing`、tag、Release 或签名密钥；仍不声明普通正式支持，不与普通客户端共机安装，且每次发布记录安全责任人、支持截止日期和实机升级结果，见 `ARCHITECTURE.md`、`docs/release-process.md`、`AGENTS.md` | "这次改动是否仍来自 main 的独立 flavor？CI 是否错误依赖内网可达？最终上传是否由服务端按历史最高版本把关？验证包与签名发布包是否分离？是否误用了普通 target、安装身份、密钥、production/testing、tag、Release 或 latest.json？" |
| **HIS 结果通道只用 WebSocket 还是保留长轮询兜底** | 初版轮询 -> 增加 WebSocket 并保留 poll -> 2026-06-12 同时删除 SDK 轮询与 Rust poll 路由 -> 2026-06-22 为解决旧 HIS 回写漏收只恢复 SDK 轮询和文档、未恢复服务端路由，形成半套 fallback，并在桌面端退出后持续产生 `handshake + poll` 失败请求 | 已收敛为 `/api/consultation/events/ws` 唯一结果通道；SDK 仅做带 `event.id` 的 WebSocket 重连，离线握手采用指数退避，禁止恢复 `/api/consultation/events/poll`，见 `ARCHITECTURE.md` 交互目标、`api.md` 6.4 与 `AGENTS.md` Review 门禁 | "这是在增强 WebSocket 重连，还是准备重新引入第二套结果通道？若要恢复轮询，是否有明确人工批准并同步恢复服务端契约、测试和全部文档？" |
| **产品品牌与历史技术标识是否同步改名** | 产品从“智医助理 / MedHermes”正式定稿为“全医慧助（PCIE）/ Primary Care Intelligent Expert”；旧名称同时承担 UI 品牌和已发布 SDK、深链、Header、Bundle Identifier，Windows MSI 又曾由不同 `productName` 隐式生成三套升级码，导致改名后新旧应用并存 | 用户可见品牌统一为“全医慧助（PCIE）”；SDK、深链、Header、Bundle Identifier 继续兼容旧标识；release 产物内部名保持 `PCIE`，Windows 通过自定义 WiX 模板展示中文名称，固定 `PCIE` 主升级码并额外迁移 `MedHermes` / 早期中文安装线，见 `PRODUCT.md`、`ARCHITECTURE.md` 与 `AGENTS.md` 硬约束 #9/#11 | "改的是用户可见品牌，还是内部产物/升级身份或既有 SDK 契约？是否保持稳定主升级码并覆盖全部历史安装线迁移？" |
| **main 与 two-chronic-diseases 是否共享发布版本线** | 两个方向从共同基点分叉后分别产生了 `1.2.99` 与 `1.3.x` tag；若继续按各自当前版本递增或复用同名 tag，会让 updater 面对版本倒退、旧 draft 和跨分支产物混合 | 已收敛为 `main` 是当前仓库唯一正式发布线并从 `1.4.0` 继续；`feature/two-chronic-diseases` 不合并回 main，后续移出当前仓库，不再占用当前正式通道和版本号，见 `ARCHITECTURE.md` 更新链路与 `AGENTS.md` 硬约束 #10 | "这次发版是否来自 main、版本是否高于全部历史公开版本、是否错误复用了另一分支的 tag / draft / 更新清单？" |
| **空病史字段是否允许自动补阴性模板** | 早期为追求病历字段完整，Builder 会在缺少询问或查体依据时自动补大段“否认……”和“未见异常”模板；随后为避免 AI 建议伪装成患者事实，曾收紧为空字段只显示未记录。2026-08-14 客户进一步明确允许既往史、个人史、家族史使用统一模板，也允许为了病例相关性和书写完整性生成候选阴性内容；首版为模板字段增加“模板预制”标签，后续走查认为该提示增加正文噪声；随后又为重点候选加入逐项三选一确认和回写阻断，实际增加了医生操作与状态复杂度 | 已收敛为：三类标准模板仍可进入可编辑草稿，内部继续识别模板以支持候选去重与安全规则，但正文不显示模板来源提示；AI 候选只承担蓝色/红色阅读提示，不再要求逐项确认或阻断回写，医生发现不准确时直接编辑正文，见 `PRODUCT.md` 病历事实标记与必要补问约束及 `ARCHITECTURE.md` 共享结果页能力 | "这段模板内容是否仍可编辑且不增加来源标签？AI 候选是否只做阅读提示、避免恢复逐项确认和回写门禁？" |
| **固定病史模板整段替换还是槽位更新** | 初始病历模板以整段普通文本预制，医生或 AI 只能替换整段，PHIS 也无法可靠定位“否认高血压”等单个默认值；若仅靠字符串差异又会受标点和模板版本变化影响 | 已收敛为固定模板保留 `{体健}/{否认}/{有}` 状态槽位；只有对话、结构化答案或 HIS 既有病史中的明确事实可把对应槽位改为 `{有}`，AI 诊断不得触发；兼容正文回写时去除花括号，另以 `outpatient-record-template-changes.v1` 的 `field + slotKey` 发送已选字段变化，PHIS 可精确更新且旧版本可忽略新增对象 | "槽位变化是否有明确事实依据？是否只发送所选字段的变化？旧 PHIS 忽略结构化变化后是否仍能读取自然文本？" |
| **体格检查生命体征为空还是保留默认槽位** | 早期为避免把未测量值伪装为事实，缺少结构化生命体征时体格检查保持空白；实际 PHIS 病历模板要求 T/P/R/BP 字段默认存在，并希望对话中明确说出的数值能落入各自字段，而不是混成一段自由文本 | 已收敛为固定显示 T/P/R/BP 五个独立值槽位：未采集时保留具名占位词且不视为测量事实，明确数值才替换；回写体格检查时正文保留槽位标记，并以 `outpatient-record-physical-exam-vitals.v1` 独立发送已填写值，见 `PRODUCT.md` 与 `api.md` | "这是具名占位字段还是已测量事实？数值是否来自本次明确上下文、按五个稳定 key 独立回写并避免正文重复？" |
| **选中诊断的鉴别信息承载形态** | 最初只有“诊断鉴别”按钮并打开页面中央弹框；随后改为单行摘要 + hover 浮层，但信息被截断且依赖鼠标停留；再改为卡片内默认展开 3 条并高亮，扫读更快却长期撑高诊断卡、下推待鉴别与治疗内容，且单行入口与展开区重复；改为按钮锚定浮层后仍遗留 controller 三条截断和正文两行截断，造成“已展开却无法看全” | 已收敛为“主诊核查按钮锚定的持久浮层”：预取完成后自动打开，以文字色和字重高亮并完整展示全部核查项和每项全文，超高时只滚动清单；底部使用“已确认”文字按钮收起，不再显示滚动说明，且该动作不产生临床事实；同一上下文关闭后不再自动打开，点击核查状态入口可重开；共享结果页不再使用中央模态框，见 `PRODUCT.md` 与 `ARCHITECTURE.md` | "改动是否同时满足默认可见、全文可达、可快速扫读、不撑高卡片、可手动关闭和可重新打开？是否又恢复了条数/行数截断、背景色块或中央遮罩？" |
| **待鉴别方向是否允许医生纳入诊疗** | 初版把待鉴别方向设计为完全只读，以避免低置信疾病进入诊断、治疗和 PHIS；实际使用中医生仍可能认可该方向并希望继续补充依据，完全只读迫使医生离开当前结果页重新操作。若直接开放选择，又会把“值得继续排查”误等同于“已经成立的正式诊断” | 已收敛为两步转入：先“纳入诊疗方向”，只记录当前结果页关注状态并引导补充依据；再由医生显式“转为正式诊断”，且必须命中标准诊断库。转入后默认作为次诊断、保留原主诊断且不自动刷新治疗；AI 正式建议仍最多 3 项，医生转入项不被截断，见 `PRODUCT.md` 诊断分区约束、`ARCHITECTURE.md` 共享结果页诊断不确定性和 `AGENTS.md` 门禁 #17 | "这是继续排查的方向还是已成立诊断？是否由医生显式转入、命中标准库、保留现有主诊断并避免自动刷新治疗？" |
| **病历事实用独立标签还是原文内标记** | 首版把已明确事实和 AI 补问集中成正文下方的胶囊与卡片，来源清楚但重复原文、破坏连续阅读；改到原文内后又在每个候选片段末尾重复“AI补充·待核查”，长病历中标签密度过高；随后曾用 `AI` 表示一般项、`!` 表示重点项，符号切换又增加了一层学习成本；统一为 `AI` 后，逐项确认浮窗、底部汇总和重点项门禁仍给医生增加额外操作；完全取消候选详情后，点击只存在于阅读层的 AI 文本又会误入字段编辑并隐藏该文本；恢复只读详情后，医生仍无法对单条 AI 内容微调或移除；仅提供“调整 → 应用”及“纳入病历”后，页面仍并存“已在正文”和“只读提示”两套回写语义；最终移除确认机制时，独立阅读层继续被排除在字段值外，导致医生看到的 AI 内容不会随一键回写提交 | 已收敛为“原文内 AI 来源标记 + 默认进入病历草稿 + 非阻断核查参考”：所有 AI 候选统一使用 `AI` 标记与虚线，一般项为蓝色、重点项为红色；候选生成及旧缓存恢复时经质控、语义去重后直接合并进对应可编辑字段，并随医生所选字段回写，不再存在独立阅读层和“纳入病历”动作；浮层只保留复制、调整和移除，调整或移除直接作用于正式字段但不产生确认状态或回写门禁，见 `PRODUCT.md` 病历事实标记与必要补问约束及 `ARCHITECTURE.md` 共享结果页能力 | "所有带 AI 标记的可见文字是否都已属于字段值并随所选字段回写？是否仍保留来源标记、幂等去重、调整和移除，同时避免恢复确认门禁？" |
| **病例字段与推荐项是否保留独立反馈入口** | 早期为收集 AI 原文与医生修正差异，在每个病历字段旁加入反馈按钮和悬浮弹层；实际运行中医生没有使用，按钮反而遮挡正文并迫使字段长期预留右侧空间；字段级入口移除后，诊断 / 治疗项仍保留反馈按钮，但继续占用高频结果页操作区 | 当前阶段结果页统一隐藏字段级和诊断 / 治疗项级反馈 UI；字段级草稿与提交接线保持移除，推荐项反馈状态与提交逻辑暂时保留以便恢复，综合问题统一由工作区顶栏主动反馈承接；字段修改判断继续用于日志与系统文本基线保护，见 `PRODUCT.md` 语音问诊反馈闭环约束与 `ARCHITECTURE.md` 反馈职责 | "这是医生真正使用的推荐反馈，还是准备重新把反馈控件放回高频病历结果页？" |
| **一键回写成功后是否自动弹出问题反馈** | 早期成功回执后强制进入整页评价浮层，医生必须提交反馈或点击“暂不反馈”才能结束结果页；实际门诊中这一额外步骤打断连续操作，且项目已有工作区顶栏主动问题反馈入口 | 已收敛为成功回执后提交最终日志并直接收起结果页，不再默认弹出问题反馈或整页评价；当前阶段诊断 / 治疗项级反馈入口也默认隐藏，综合问题由顶栏主动入口承接，见 `PRODUCT.md` 一键回写成功收尾约束 | "反馈是医生主动行为，还是被放在高频回写主流程中的强制收尾步骤？" |
| **一键回写是整张提交还是允许部分范围** | 原 `record-confirmed` 固定携带完整门诊病历、诊断和全部已选医嘱；医生反馈实际工作中需要保留 HIS 已有部分内容，只回填本次需要的字段或分组。首版部分回写统一省略未选分组，但 PHIS 对缺失 `orderList` 直接执行 `forEach` 并报错；院端确认 `orderList: []` 在 `writebackScope.orderTypes = []` 时不会清空既有医嘱。动态“回写已选内容”文案还曾导致相邻入口和浮层跳动 | 已收敛为“一条 batch 请求 + 显式 writebackScope + 病历/诊断未选即省略 + orderList 始终为数组 + 固定一键回写 CTA”：门诊病历七字段可分别选择，诊断、用药、检查检验处置按组选择；没有任何医嘱范围时传 `orderList: []`，PHIS 依据空 `orderTypes` 保持原医嘱；只校验实际选择范围，未选内容不计为拒绝建议，见 `PRODUCT.md` 与 `api.md` record-confirmed | "当前 payload 中哪些字段以缺失表示保持原值？`orderList` 是否始终为数组并与 `orderTypes` 一致？范围状态是否会通过改变主 CTA 尺寸造成布局跳动？" |
| **治疗推荐选中态使用多少视觉信号** | 初版只使用复选圆点；为加强可见性曾叠加蓝色卡片边框、外圈阴影、浅底和蓝色属性胶囊，检查检验按临床问题连续分组后，这些信号与分组左侧蓝线、优先标签和操作按钮同时出现，实际选中多项时形成连续高对比线框，阅读显得杂乱；第一轮只把选中边框降成中性色，实际截图中整组外框、项目外框和多个胶囊按钮仍然存在，轮廓数量没有明显减少；进一步只保留分组左线后，分组语义和项目选中语义又共用同一条竖线，状态区分仍不够直接 | 已收敛为“浅色组头 + 无框项目行 + 三项轻量选中信号”：分组依靠低饱和组头和间距建立层级，项目不绘制四周边框，多项目仅保留内部轻分隔；执行科室与次级操作使用低权重文字；选中项使用蓝色实心勾、项目行窄蓝色左标线和极浅蓝底，三者不得再叠加光圈、重阴影或整卡蓝框，见 `PRODUCT.md` 视觉基线与诊疗建议扫读约束 | "本次是在改变边框颜色，还是实际减少轮廓数量？分组识别与项目选中是否各用清晰但克制的信号？同屏每个项目除主要展开入口外是否仍存在多个胶囊外框？" |
| **风险胶囊是简短摘要还是完整管理说明** | 最初 prompt 限制风险提示不超过 20 字；后续在病史字段改动中移除字数限制，模型开始输出疾病定义、长期治疗和并发症等完整说明；多慢病识别范围扩大后信息密度进一步上升 | 已收敛为接诊摘要：慢病使用“疾病名称：一个核心关注点”，单项原则上不超过 24 字；模型与客户端纯规则双重收敛，不在 UI 层截断语义，见 `PRODUCT.md` 风险胶囊文案约束与 `ARCHITECTURE.md` 患者上下文基线 | "本次是在胶囊中提高扫读性，还是需要另外承载完整管理知识？是否会再次去掉字数与句式约束？" |
| **医生上位医嘱是停在待匹配还是结合对话补全** | 为避免目录外项目进入回写，初版只按“B超、CT、化验”等原始名称做保守目录匹配，未命中即待医生手动更换；实测医生已在对话中给出症状、部位和诊断线索，再次手工查目录增加操作，而直接接受任一名称相近候选又会把 AI 推测伪装成医生原话 | 已收敛为“实时目录内上下文补全 + 医生勾选确认”：名称明确时确定性匹配；上位表达结合本次完整对话、病历和正式诊断，从本次实时目录候选中只接受 high / medium 置信映射并补齐临床目标，保留原话和补全依据、默认未选中；低置信或无唯一候选继续待匹配。该能力只补全医生已明确提出的医嘱类型，不得演变为目录外或自主新增方案，见 `PRODUCT.md` 语音问诊治疗方案约束与 `ARCHITECTURE.md` 渐进结果架构 | "这是医生已经表达的上位医嘱，还是 AI 自主推荐？是否只从本次实时目录选择、保留原话、显示补全依据并等待医生勾选？" |
| **普通语音结果是串行稳定后开放还是渐进可编辑** | 最初等待完整结构化 JSON 后一次展示；为改善感知加入 NDJSON 分区，但每个 partial 仍整页 reset，complete 后又串行等待治疗生成和通用病历候选，实测形成约 11 秒主抽取 + 10 秒治疗 + 8 秒晚到补充，并可能因正文变化重复请求主诊核查。随后改成治疗后台并行，却出现“诊断仍刷新、检查已先展示”和分支按返回顺序跳动；有效的纯待鉴别结果还会被误判为诊断缺失而追加第二次请求 | 已收敛为“同轮次首次 reset + 后续增量 patch + 计算并行、呈现有序”：独立版本化 Prompt 同次返回核心病历、AI 候选、诊断和诊疗路由；已选正式诊断与路由就绪即并行启动治疗，核心病历稳定后开放编辑，治疗完成前只禁用一键回写。前台先稳定诊断，再按固定药品 / 检查 / 检验槽位原位展示分支状态，单分支整组落地；纯待鉴别属于有效结果，不补发诊断、不触发治疗。缺少候选分区才走一次稳定态兜底，只有旧结果完全缺失诊断分区才允许一次诊断恢复；已有有效流式分区后不重放整次请求。该状态机只用于普通语音，慢病复诊和报告回诊保持独立策略，见 `PRODUCT.md` 语音问诊治疗方案完整性与病历事实约束、`ARCHITECTURE.md` 渐进结果架构及 `AGENTS.md` 门禁 #23 | "同一生成轮次是否只首次 reset？诊断未稳定时是否暴露了 AI 治疗？有效待鉴别是否被误触发第二次诊断？治疗是否后台并行但按固定槽位原位呈现，并继续经过实时目录 / 药品定稿？慢病与报告回诊是否仍走原渠道？" |
| **动态病历模板当前值是占位提示还是字典选择** | 门诊模板首轮解析把 `.placeholder / DEFAULTVALUE` 与字段当前 `TEXT` 都视为可能需要清空的默认文案；保留完整 `BINDINGDATA` 后又把模板当前合法 `VALUE + TEXT` 当成模型缺失或非法输出的可用兜底。真实病例模拟后客户进一步明确：模板当前选择只能说明模板源状态，每个生效字典字段仍必须由本次模型重新显式返回，无法匹配时应留空让医生补选 | 已收敛为三类语义分离：HTML 占位 DOM 与 JSON `DEFAULTVALUE` 仅供提示；只有结构定义 `BINDINGDATA[].VALUE/TEXT` 形成权威字典；结构定义当前 `VALUE + TEXT` 与 HTML 当前显示值必须一致，但只作为模板源元数据，不进入分析草稿。模型逐个返回生效字典字段，漏项属于模型输出协议错误；已显式返回空值或模板外值成为医生待选状态，不回退当前项；医生补齐后最终值必须唯一命中，回参同时携带显示文本和 `{ value, text }`。不适用字段由上游渲染器从 `targetFieldIds` 省略，见 `PRODUCT.md`、`ARCHITECTURE.md` 11.1 与 `api.md` 6.3C | "这是提示、字典定义、模板源当前值、模型本次结果还是医生最终选择？模型是否逐字段显式返回，非法值是否留空而不是回退当前项？" |
| **动态病历模板适用字段由 PCIE 推断还是渲染器指定** | 首版允许省略 `targetFieldIds`，由 PCIE 根据 `data-ai-suitable`、字段映射和患者性别保守推断；独立病例实验发现模板脚本会同时隐藏“月经史”和“婚育史”，而 PCIE 的内置性别规则只识别月经字段，导致男性病例仍被怀孕、哺乳字典阻断。随后在“PCIE 解释各模板脚本”和“上游渲染后传有效字段”之间再次确认边界 | 已收敛为上游渲染器权威：正式 HTTP/SDK 调用必须传非空 `targetFieldIds`，只包含当前实际显示且需要分析的可写字段；PCIE 不执行 `relationscript`、不按患者属性、字段映射、章节或模板标记扩充范围，未知/只读目标整次拒绝；桌面端不再提供手工选择字段的本地实验入口，见 `PRODUCT.md` 门诊模板 MVP、`ARCHITECTURE.md` 11.1 与 `api.md` 6.3C | "当前字段范围来自已经执行模板联动的渲染器吗？是否有人准备恢复 PCIE 自动推断、默认全选或静默忽略无效 targetFieldIds？" |
| **门诊动态模板实验协议是否背负早期兼容** | 初版借用了住院入口的 `htmlContent` 字段名，并为未定稿来源增加 `data-options`、JSON 对象包装、大小写别名及多种属性名兜底；随后又因错误理解样本，把 HTML 与章节 JSON 当作两种可替代模板源并收敛为单个 `templateSource`。真实文件核验确认二者其实是同一模板的渲染实例与结构定义：HTML 无法独立提供完整字典，JSON 也无法独立保留真实布局 | 已收敛为再次不兼容迁移：正式门诊入口只接受成对必填的 `templateHtml + templateDefinition`，按稳定章节/字段 ID 严格合并；删除 `htmlContent / templateSource / sourceFormat`、HTML 字典属性、JSON 单源预览和所有旧解析路径。任一半缺失、字段身份或当前值不一致直接失败；未经用户明确指定兼容版本与退出条件，不再加入双读双写、别名或兜底，见 `AGENTS.md` 硬约束、`PRODUCT.md` 门诊模板对规范与 `api.md` 6.3C | "当前实现是否把同一模板的两部分误当成二选一？是否仍保留任一单源入口、旧字段或静默拼补路径？" |
| **门诊模板只在桌面实验台查看还是同步后台留痕** | 首版将模板上传理解为桌面端本地联调动作，明确不保存模板，也没有把解析器到后台管理页的调用链纳入实现；实际接入核验需要后台同时看到客户端真实传入的模板原文和确定性解析结果。随后确认桌面端本地实验台不是产品能力，应直接删除而非隐藏或兼容 | 已收敛为仅正式 HIS 分析前强制登记：Bridge/SDK 传入模板对后，客户端先经签名 `/v1/client/outpatient-emr/templates/snapshots` 上传渲染 HTML、结构定义 JSON 与完整合并解析快照，成功后才调用模型；后台“病历模板 → 门诊解析记录”只读分别展示两份原文和完整解析 JSON，并按机构与模板对 hash 去重。桌面端不提供模板上传、粘贴、字段勾选或本地结果预览；患者、就诊、病历上下文、模型值和医生值不进入快照，见 `PRODUCT.md`、`ARCHITECTURE.md`、`api.md` 与 `pcie-server/API.md` | "后台是否同时看到模型调用前的两份模板原文与合并解析版本？是否错误上传了患者或生成内容？桌面端是否又出现了本地模板入口？" |
| **门诊模板解析是每次重做还是按版本复用** | 初版在客户端先完成 HTML/JSON 严格解析，再把快照发到后台；后台虽然按 hash 去重落库，但去重发生在解析之后，因此相同模板每次就诊仍重复解析。随后明确首次模板或模板更新才创建解析，已存在版本必须直接调用历史解析 | 已收敛为内容版本前置查询：客户端先计算模板对 hash，再按当前机构、`templateId + templateHash` 调用签名 `/snapshots/resolve`；命中时只恢复完整字段快照，不再解码定义、配对模板或重复登记；未命中时才严格解析并登记新 hash。查询失败、命中快照损坏和登记失败均阻断模型，不以本地重解析或旧协议兜底，见 `PRODUCT.md`、`ARCHITECTURE.md`、`api.md` 与 `pcie-server/API.md` | "本次模板内容真的变化了吗？是否在查历史解析之前就做了完整解析，或在缓存查询失败时偷偷重复解析？" |
| **门诊请求身份由客户端修正还是由 HIS 完整负责** | 早期 SDK 会在缺少 `requestId` 时自动生成，并在 SDK、Rust Bridge 和页面任务身份层分别修剪、去重；结果监听还会从事件 envelope 或兼容顶层字段补齐 payload 身份。相同 requestId 即使病历上下文或患者事实变化，也可能继续复用旧分析 | 已收敛为严格端到端身份：HIS 必须显式提供 requestId，身份字段和 targetFieldIds 原值校验、不修正；目标字段重复或含首尾空白直接失败。幂等恢复比较模板、目标集合、patient 和 recordContext 的完整稳定快照；确认与取消只读取正式 payload 自身的完整字段，不从事件外层补齐，见 `PRODUCT.md`、`ARCHITECTURE.md`、`api.md` 与 SDK 文档 | "本次请求是否被任何一层静默改写？相同 requestId 的全部分析输入是否真的相同？结果匹配是否依赖 payload 自身的完整身份？" |

---

## 新增模板

```markdown
| **决策点** | 摇摆历史 | 当前状态 | "改动前必须问清的问题？" |
```

如果单行表格放不下，使用下面的展开模板：

```markdown
### DRIFT-XXX: 决策点

- **摇摆历史**: 从 A 到 B，再到 C，期间为什么反复。
- **当前状态**: 当前代码、产品或规则采用的立场。
- **改动前问**: 后续 agent 在动手前必须向人确认的问题。
- **收敛位置**: 如已固化，注明 `ARCHITECTURE.md` / `PRODUCT.md` / `AGENTS.md` 的位置；未固化则写"未收敛"。
```


### DRIFT: 慢病本次诊疗范围与患者既往史边界

- **摇摆历史**: 为防止多慢病整单配药，将病历生成统一限制到 scoped candidate；试点发现只配高血压药时糖尿病历史也被裁掉，模板反而生成否认病史。
- **当前状态**: 本次诊疗继续限于医生已选慢病；既往史独立保留同患者完整已知慢病，不能由本次是否配药决定。模型不覆写确定性既往史基线，医生可编辑。
- **改动前问**: 此字段描述本次诊疗范围，还是患者真实历史事实？
- **收敛位置**: AGENTS.md Review 门禁 #14、PRODUCT.md 3B、ARCHITECTURE.md 3.1。


### DRIFT: 症状性工作诊断只推荐检查与医生主动请求对症用药

- **摇摆历史**: 为避免在病因不明时过早用药，工作诊断自动只走检查/检验；临床反馈缺少检查条件或不需检查时无法继续用药，通用刷新又重复原路由。
- **当前状态**: 自动路由保持原限制；新增医生显式、单次的现有信息药品评估，工作诊断仅允许对症方案，缺少关键依据的具体药品继续暂缓，不升级诊断、不删除检查。
- **改动前问**: 这是自动启动治疗还是医生主动请求？是否按具体药品证据限制，而非把全部用药无条件放开？
- **收敛位置**: PRODUCT.md 基于现有信息主动推荐用药、ARCHITECTURE.md 对应架构、AGENTS.md 门禁 #26。

### DRIFT: 肺部查体默认书写候选

- **摇摆历史**: 医生原始基础查体为“双肺呼吸音粗”，优化方案曾改为“双肺呼吸音清”；用户再次向医生确认后要求恢复“双肺呼吸音粗”。
- **当前状态**: 新生成的默认书写候选使用“双肺呼吸音粗”，仅为带 AI 来源标记的可编辑模板，不将其定义成正常体征或已确认事实。本次明确查体及医生编辑优先，旧正文不自动迁移。
- **改动前问**: 改的是默认候选还是本次明确听诊结果？是否保持默认书写与临床事实的边界？
- **收敛位置**: PRODUCT.md 按本次就诊选择体格检查、AGENTS.md 场景查体补全门禁、ARCHITECTURE.md 场景查体项目库与证据隔离。

### 2026-09-22：普通语音候选与诊断的流式顺序收敛

前期普通语音先返回病史和 AI 书写候选再返回诊断；端到端实测显示病史 3.301s、候选 5.451s、诊断 7.406s、路由 8.053s，药品模型直到 8.222s 才开始并在 14.056s 完成，而结果构建累计仅 14ms。因此保留核心病历/已知病史优先，把书写候选移到诊断和路由之后，使治疗与剩余主流重叠；不拆成第二次候选请求，不减少临床依据或放松正式诊断门禁。当前顺序以 `ARCHITECTURE.md`“普通语音诊断优先输出”和 `PRODUCT.md` 对应补充为准，实际节省时间以同病例新日志对比为准。

### 2026-09-22：低置信路由从自动扩展收敛为证据边界不变

旧规则将低置信路由自动展开成药品/检查/检验，但保留原 defer/skip，导致药品同时处于立即推荐和暂缓集合，并把模型置信度波动放大为分支变化。现收敛为置信度不改变集合、集合互斥、diagnostic_first 暂缓药品，允许无自动推荐；不以统一全部推荐或缓存旧结果掩盖不确定性。只对普通语音识别/修复设置低温度，临床事实不充分时继续沿用症状性工作诊断与医生主动评估边界，见 ARCHITECTURE.md“普通语音诊疗路由稳定性”。
## 2026-09-23：多慢病历史药品归类从接诊预请求合并进慢病生成流

- **此前方案**：接诊候选发布后立即调用一次快速模型归类同次多慢病的共享处方；医生部分选择慢病时，胶囊内部等待归类 ready / failed 后再进入结果页。
- **调整原因**：独立请求只服务于“同次多慢病、无处方诊断关联、医生部分选择”的窄场景，却会在医生较快点击时形成额外等待，并与随后同样使用快速模型的慢病病历生成重复承担上下文理解。
- **收敛方案**：胶囊确认范围后立即进入共享结果页；歧义药品随同一次慢病 NDJSON 请求发送，由新增 `medication_scope` 事件先完成稳定 item id 到候选 condition id 的归类，再生成核查项和推荐药品。本地继续执行白名单、所选诊断和 high / medium 置信度门禁；缺失、低置信、越权或流式失败均不继承歧义处方，也不追加第二次请求。
- **收敛位置**：`AGENTS.md` 多慢病复诊范围门禁、`PRODUCT.md` 3B、`ARCHITECTURE.md` 3.1、`CODE_MAP.md` 慢病归类入口。
